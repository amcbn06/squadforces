"""Judge platforms. One module per platform (cf.py, atc.py, kn.py, cses.py, other.py), each defining a
`Platform` subclass; registry.py lists them. Import the registry, not the modules, from the rest of the app."""
